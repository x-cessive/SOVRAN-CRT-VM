# configs/

Configuration examples for the lab. Empty until validated against a real run.

Intended future contents: sanitized libvirt domain XML, modprobe/`vfio-pci`
binding configs, kernel cmdline snippets. Sanitized per `../docs/SECURITY.md`;
annotated with the phase and date they were proven against.
