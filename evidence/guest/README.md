# evidence/guest/

Guest-side evidence: sanitized VM configs, guest `lspci` view of the passed-through GPU. No guest exists yet.

Sanitize before commit (see `../../docs/SECURITY.md`); document redactions.
