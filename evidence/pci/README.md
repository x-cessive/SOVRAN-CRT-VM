# evidence/pci/

PCI enumeration captures: `lspci -nn`, `lspci -nnk`, `lspci -vv`, `lspci -t` for the legacy GPU and its functions. No captures yet.

Sanitize before commit (see `../../docs/SECURITY.md`); document redactions.
