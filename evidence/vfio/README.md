# evidence/vfio/

VFIO binding state: which driver owns each GPU function, vfio device nodes. No captures yet — nothing bound.

Sanitize before commit (see `../../docs/SECURITY.md`); document redactions.
