# evidence/iommu/

IOMMU topology: group membership for the GPU (`/sys/kernel/iommu_groups/`), `dmesg` IOMMU init lines. No captures yet.

Sanitize before commit (see `../../docs/SECURITY.md`); document redactions.
