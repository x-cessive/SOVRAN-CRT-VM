# diagrams/

Diagrams for the lab. The canonical topology diagram lives in the top-level
README.md (ASCII). Future additions (IOMMU group maps, signal-path diagrams)
go here as text-first diagrams (ASCII/Mermaid) so they stay reviewable in
plain git diffs.
